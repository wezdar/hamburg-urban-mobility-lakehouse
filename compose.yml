services:
  pipeline:
    build: .
    command: ["backfill", "--start", "2026-08-08", "--end", "2026-08-09"]
    volumes:
      - mobility-data:/app/data

  quality:
    build: .
    command: ["quality"]
    volumes:
      - mobility-data:/app/data
    profiles: ["quality"]

volumes:
  mobility-data:
